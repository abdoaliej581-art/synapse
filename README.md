# Synapse

A modular AI system built in phased development.

## Branches

- `main` — production-ready releases
- `develop` — main development branch
- `phase-*` — feature branches for each development phase
